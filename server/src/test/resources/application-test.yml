spring:
  datasource:
    url: jdbc:h2:mem:smetrix_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test

jwt:
  secret: test-only-secret-key-that-is-longer-than-thirty-two-bytes
  expiration-ms: 900000
  refresh-expiration-ms: 2592000000

fgis:
  enabled: false
  import-key: test-import-key

alerts:
  admin-email:
